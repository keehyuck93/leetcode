/**
 * @param {number[]} nums
 * @return {number}
 */
var maxProduct = function(nums) {
    let max = nums[0];
    let curMax
    for (let i=0; i < nums.length; i += 1) {
        
    }

    return max;
};

console.log(maxProduct([1, -1, 2, -2, 5, -3]));